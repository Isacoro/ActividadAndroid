<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.isabel.actividadandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ActividadAndroid"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ActividadAndroid">
        <activity android:name=".movies.filtroMovies.view.FiltroMoviesView"></activity>
        <activity android:name=".movies.listMovies.view.DetalleMovieView" />
        <activity android:name=".MovieAdapter" />
        <activity android:name=".movies.listMovies.view.ListMoviesActivity$DetalleMovieView" />
        <activity android:name=".movies.listMovies.view.ListMoviesActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>